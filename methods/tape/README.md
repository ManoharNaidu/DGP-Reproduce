# tape

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py tape` (never committed).
Provenance, adaptation notes and status: `research/baselines/tape.md` and `methods/README.md`.
