# pmp

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py pmp` (never committed).
Provenance, adaptation notes and status: `research/baselines/pmp.md` and `methods/README.md`.
