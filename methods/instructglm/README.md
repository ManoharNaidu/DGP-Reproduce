# instructglm

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py instructglm` (never committed).
Provenance, adaptation notes and status: `research/baselines/instructglm.md` and `methods/README.md`.
