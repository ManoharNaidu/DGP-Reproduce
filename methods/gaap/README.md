# gaap

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py gaap` (never committed).
Provenance, adaptation notes and status: `research/baselines/gaap.md` and `methods/README.md`.
