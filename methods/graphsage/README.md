# graphsage

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py graphsage` (never committed).
Provenance, adaptation notes and status: `research/baselines/graphsage.md` and `methods/README.md`.
