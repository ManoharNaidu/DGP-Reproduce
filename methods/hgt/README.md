# hgt

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py hgt` (never committed).
Provenance, adaptation notes and status: `research/baselines/hgt.md` and `methods/README.md`.
