# flag

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py flag` (never committed).
Provenance, adaptation notes and status: `research/baselines/flag.md` and `methods/README.md`.
