# graphgpt

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py graphgpt` (never committed).
Provenance, adaptation notes and status: `research/baselines/graphgpt.md` and `methods/README.md`.
