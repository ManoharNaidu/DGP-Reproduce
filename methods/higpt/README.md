# higpt

Official code is fetched into `upstream/` at a pinned commit by `python scripts/fetch_baselines.py higpt` (never committed).
Provenance, adaptation notes and status: `research/baselines/higpt.md` and `methods/README.md`.
